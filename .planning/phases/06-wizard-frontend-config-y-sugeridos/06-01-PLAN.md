---
phase: 06-wizard-frontend-config-y-sugeridos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/reposiciones/config.test.tsx
  - src/test/reposiciones/table.test.tsx
  - src/pages/reposiciones/types.ts
  - src/pages/reposiciones/api.ts
  - src/pages/reposiciones/hooks.ts
  - src/pages/reposiciones/index.tsx
  - src/lib/pages.ts
  - src/App.tsx
  - firestore.rules
autonomous: false
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, APPR-01, APPR-02, APPR-03, APPR-04]

must_haves:
  truths:
    - "TypeScript types mirror all backend Pydantic response models exactly"
    - "API layer covers all 7 backend endpoints with resilientFetch pattern"
    - "React Query hooks exist for locations, vendors, sales status polling, calculate mutation, and Firestore config"
    - "Route /reposiciones is registered in PAGE_REGISTRY and App.tsx"
    - "Test stubs exist for all CONF and APPR requirements"
    - "Firestore rules allow replenishment_config read/write scoped to user UID"
  artifacts:
    - path: "src/pages/reposiciones/types.ts"
      provides: "All TS interfaces for the module"
      contains: "ProductResult"
    - path: "src/pages/reposiciones/api.ts"
      provides: "API functions for all backend endpoints"
      contains: "calculateReplenishment"
    - path: "src/pages/reposiciones/hooks.ts"
      provides: "React Query hooks + Firestore config hook"
      contains: "useReplenishmentConfig"
    - path: "src/lib/pages.ts"
      provides: "Page registry entry"
      contains: "/reposiciones"
    - path: "src/App.tsx"
      provides: "Lazy route"
      contains: "Reposiciones"
    - path: "src/test/reposiciones/config.test.tsx"
      provides: "Test stubs for CONF-01 through CONF-05"
      contains: "CONF-01"
    - path: "src/test/reposiciones/table.test.tsx"
      provides: "Test stubs for APPR-02 through APPR-04"
      contains: "APPR-02"
    - path: "firestore.rules"
      provides: "Firestore security rules including replenishment_config"
      contains: "replenishment_config"
  key_links:
    - from: "src/pages/reposiciones/api.ts"
      to: "/api/reposiciones/*"
      via: "resilientFetch"
      pattern: "resilientFetch.*api/reposiciones"
    - from: "src/pages/reposiciones/hooks.ts"
      to: "src/pages/reposiciones/api.ts"
      via: "React Query hooks calling API functions"
      pattern: "import.*from.*\\./api"
---

<objective>
Create the foundation layer for the /reposiciones module: Wave 0 test stubs, TypeScript types mirroring backend models, API functions for all endpoints, React Query hooks, Firestore config persistence hook, page/route registration, and Firestore rules update for the new replenishment_config collection.

Purpose: All subsequent plans depend on these contracts. Types, API layer, and hooks must exist before any UI component can be built. Test stubs enable automated verification from the start.
Output: Test stubs in src/test/reposiciones/, types.ts, api.ts, hooks.ts files in src/pages/reposiciones/, route registration in pages.ts and App.tsx, and updated firestore.rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wizard-frontend-config-y-sugeridos/06-CONTEXT.md
@.planning/phases/06-wizard-frontend-config-y-sugeridos/06-RESEARCH.md

<interfaces>
<!-- Backend endpoint contracts from backend/routers/reposiciones.py -->

GET /api/reposiciones/locations -> [{name: str, id: str}]
GET /api/reposiciones/vendors -> [{name: str, product_count: int}]
GET /api/reposiciones/inventory?location_id=X&vendors[]=Y -> product list
POST /api/reposiciones/sales/refresh?date_range_days=180 -> {status, message, job_id?}
GET /api/reposiciones/sales/status -> {status: idle|running|completed|failed, object_count?, last_refreshed?, sku_count?, error?}
GET /api/reposiciones/sales/data -> {data: {...}, last_refreshed, sku_count}
POST /api/reposiciones/calculate -> {products: ProductResult[], vendor_summary: VendorSummaryResult[], stats: ReplenishmentStatsResult, draft_id: str}

CalculateRequest body: {location_id: str, vendors: list[str]|None, lead_time_days: int, safety_factor: float, date_range_days: int}

<!-- Existing API pattern from src/pages/gift-cards/api.ts -->
import { resilientFetch } from "@/lib/resilient-fetch";
const API_BASE = import.meta.env.VITE_API_URL ?? "";
async function handleResponse<T>(response: Response): Promise<T> { ... }

<!-- Existing hooks pattern from src/pages/gift-cards/hooks.ts -->
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";

<!-- Firestore pattern from src/pages/devoluciones/hooks.ts -->
import { doc, getDoc, setDoc, serverTimestamp } from "firebase/firestore";
import { db } from "@/lib/firebase";

<!-- PAGE_REGISTRY pattern from src/lib/pages.ts -->
{ path: "/reposicion", label: "Reposicion", description: "Gestion de reposicion", icon: Package, workspace: "operaciones" }

<!-- App.tsx lazy pattern -->
const Reposicion = lazyWithReload(() => import("./pages/Reposicion"));
<Route path="/reposicion" element={<Reposicion />} />

<!-- Existing test pattern from src/test/ -->
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for CONF and APPR requirements</name>
  <files>src/test/reposiciones/config.test.tsx, src/test/reposiciones/table.test.tsx</files>
  <read_first>
    src/test/
  </read_first>
  <action>
Create `src/test/reposiciones/` directory.

**config.test.tsx** — Test stubs for CONF-01 through CONF-05:

```typescript
import { describe, it, expect, vi } from "vitest";

describe("Reposiciones — Config (CONF-01 to CONF-05)", () => {
  describe("CONF-01: Location dropdown", () => {
    it("should render a location dropdown populated from useLocations hook", () => {
      // Stub: will be implemented when ConfigPanel exists (Plan 02)
      expect(true).toBe(true);
    });
  });

  describe("CONF-02: Vendor multi-select", () => {
    it("should render a multi-select with 'Todos' as default", () => {
      expect(true).toBe(true);
    });

    it("should allow selecting specific vendors", () => {
      expect(true).toBe(true);
    });
  });

  describe("CONF-03: Lead time input", () => {
    it("should render lead time input with default 14 and range 1-90", () => {
      expect(true).toBe(true);
    });
  });

  describe("CONF-04: Date range input", () => {
    it("should render date range in months with default 6 and range 1-12", () => {
      expect(true).toBe(true);
    });

    it("should convert months to days (months * 30) on submit", () => {
      expect(true).toBe(true);
    });
  });

  describe("CONF-05: Config persistence", () => {
    it("should save config to Firestore on calculate", () => {
      expect(true).toBe(true);
    });

    it("should load saved config from Firestore on mount", () => {
      expect(true).toBe(true);
    });
  });
});
```

**table.test.tsx** — Test stubs for APPR-02 through APPR-04:

```typescript
import { describe, it, expect, vi } from "vitest";

describe("Reposiciones — Table (APPR-02 to APPR-04)", () => {
  describe("APPR-02: Suggestions table with search and filter", () => {
    it("should render table with all required columns", () => {
      expect(true).toBe(true);
    });

    it("should filter products by search term (SKU, title, vendor)", () => {
      expect(true).toBe(true);
    });

    it("should filter products by urgency level", () => {
      expect(true).toBe(true);
    });
  });

  describe("APPR-03: Inline edit and delete", () => {
    it("should allow inline editing of suggested_qty", () => {
      expect(true).toBe(true);
    });

    it("should allow deleting SKU rows", () => {
      expect(true).toBe(true);
    });

    it("should preserve overrides across recalculations", () => {
      expect(true).toBe(true);
    });
  });

  describe("APPR-04: Vendor summary reflects edits", () => {
    it("should recompute vendor summary from effective products", () => {
      expect(true).toBe(true);
    });

    it("should update totals when overrides or deletions change", () => {
      expect(true).toBe(true);
    });
  });
});
```

These are passing stubs (expect(true).toBe(true)) that document the required behaviors. They will be fleshed out with real assertions when the components are created in Plans 02 and 03.
  </action>
  <verify>
    <automated>npx vitest run src/test/reposiciones 2>&1 | tail -10</automated>
  </verify>
  <done>src/test/reposiciones/config.test.tsx has stubs for CONF-01 through CONF-05; src/test/reposiciones/table.test.tsx has stubs for APPR-02 through APPR-04; all stubs pass</done>
</task>

<task type="auto">
  <name>Task 1: Create types.ts + api.ts — contracts and API layer</name>
  <files>src/pages/reposiciones/types.ts, src/pages/reposiciones/api.ts</files>
  <read_first>
    src/pages/gift-cards/api.ts
    src/pages/gift-cards/types.ts
    backend/routers/reposiciones.py
    src/pages/reposicion/types.ts
  </read_first>
  <action>
Create `src/pages/reposiciones/` directory.

**types.ts** — Define these exact interfaces mirroring backend Pydantic models:

```typescript
export type UrgencyLevel = "URGENTE" | "PRONTO" | "NORMAL" | "OK";
export type Classification = "Bestseller" | "Regular" | "Slow" | "Long Tail";

export interface LocationItem {
  name: string;
  id: string;
}

export interface VendorItem {
  name: string;
  product_count: number;
}

export interface ProductResult {
  sku: string;
  title: string;
  vendor: string;
  classification: Classification;
  classification_label: string;
  sales_per_month: number;
  sales_per_week: number;
  sales_per_day: number;
  total_sold: number;
  stock: number;
  days_of_inventory: number | null;
  urgency: UrgencyLevel;
  urgency_label: string;
  reorder_point: number;
  needs_reorder: boolean;
  suggested_qty: number;
  in_transit_real: number;
}

export interface VendorSummaryResult {
  vendor: string;
  total_skus: number;
  total_units_to_order: number;
  urgent_count: number;
}

export interface ReplenishmentStatsResult {
  total_products: number;
  needs_replenishment: number;
  urgent: number;
  out_of_stock: number;
  vendors_with_orders: number;
}

export interface CalculateResponse {
  products: ProductResult[];
  vendor_summary: VendorSummaryResult[];
  stats: ReplenishmentStatsResult;
  draft_id: string;
}

export interface CalculateRequest {
  location_id: string;
  vendors: string[] | null;
  lead_time_days: number;
  safety_factor: number;
  date_range_days: number;
}

export interface ReplenishmentConfig {
  location_id: string;
  vendors: string[];
  lead_time_days: number;
  safety_factor: number;
  date_range_days: number;
  updated_at?: unknown;
}

export interface SalesStatusResponse {
  status: "idle" | "running" | "completed" | "failed";
  object_count?: number;
  last_refreshed?: string;
  sku_count?: number;
  error?: string;
}

export interface SalesRefreshResponse {
  status: string;
  message: string;
  job_id?: string;
}
```

**api.ts** — Create API functions following gift-cards/api.ts pattern exactly:

1. Import `resilientFetch` from `@/lib/resilient-fetch`
2. Define `API_BASE = import.meta.env.VITE_API_URL ?? ""`
3. Copy `handleResponse<T>` pattern from gift-cards/api.ts
4. Create these functions:
   - `getLocations(): Promise<LocationItem[]>` — GET `/api/reposiciones/locations`
   - `getVendors(): Promise<VendorItem[]>` — GET `/api/reposiciones/vendors`
   - `getSalesStatus(): Promise<SalesStatusResponse>` — GET `/api/reposiciones/sales/status`
   - `refreshSales(dateRangeDays: number): Promise<SalesRefreshResponse>` — POST `/api/reposiciones/sales/refresh?date_range_days=${dateRangeDays}`
   - `calculateReplenishment(params: CalculateRequest): Promise<CalculateResponse>` — POST `/api/reposiciones/calculate` with JSON body

5. For `refreshSales`, handle 409 specially per D-06: check if response.status === 409, parse body, and if `body.error === "OPERATION_IN_PROGRESS"`, throw `new Error(body.message || "Hay una operacion Bulk en curso en Shopify")`.

Import all types from `./types`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/pages/reposiciones/types.ts src/pages/reposiciones/api.ts 2>&1 || echo "tsc check done"</automated>
  </verify>
  <done>types.ts has all interfaces matching backend Pydantic models; api.ts has 5 exported functions covering all 5 API endpoints with resilientFetch pattern, 409 handling for sales/refresh</done>
</task>

<task type="auto">
  <name>Task 2: Create hooks.ts + register page route</name>
  <files>src/pages/reposiciones/hooks.ts, src/pages/reposiciones/index.tsx, src/lib/pages.ts, src/App.tsx</files>
  <read_first>
    src/pages/gift-cards/hooks.ts
    src/pages/devoluciones/hooks.ts
    src/lib/pages.ts
    src/App.tsx
    src/pages/reposiciones/api.ts
    src/pages/reposiciones/types.ts
  </read_first>
  <action>
**hooks.ts** — Create React Query hooks following gift-cards/hooks.ts pattern:

1. `useLocations()` — `useQuery({ queryKey: ["reposiciones", "locations"], queryFn: getLocations, staleTime: 10 * 60 * 1000, retry: 2 })` (per D-13)
2. `useVendors()` — `useQuery({ queryKey: ["reposiciones", "vendors"], queryFn: getVendors, staleTime: 10 * 60 * 1000, retry: 2 })` (per D-14)
3. `useSalesStatusPolling(enabled: boolean)` — `useQuery` with `refetchInterval` that returns 3000 when `enabled && (data?.status === "running" || !data)`, `false` otherwise. This implements the polling for D-04 cache refresh flow.
4. `useCalculate()` — `useMutation({ mutationFn: calculateReplenishment })` for POST /calculate
5. `useReplenishmentConfig(uid: string | undefined)` — `useQuery` that reads Firestore `replenishment_config/{uid}` using `getDoc(doc(db, "replenishment_config", uid))`, returns `ReplenishmentConfig | null`. `enabled: !!uid`, `staleTime: Infinity`. (per D-18, D-19)
6. `saveReplenishmentConfig(uid: string, config: ReplenishmentConfig)` — async function (not a hook) that calls `setDoc(doc(db, "replenishment_config", uid), { ...config, updated_at: serverTimestamp() })`. (per D-18)

Import types from `./types`, API functions from `./api`, firebase from `@/lib/firebase`.

**index.tsx** — Create minimal page shell (placeholder for Plan 02 to flesh out):

```typescript
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ReposicionesPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold">Reposiciones</h1>
      <Card>
        <CardHeader>
          <CardTitle>Configuracion de Reposicion</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">Modulo en construccion...</p>
        </CardContent>
      </Card>
    </div>
  );
}
```

**src/lib/pages.ts** — Add entry to PAGE_REGISTRY array (per D-03, memory feedback_new_page_nav.md). Add AFTER the existing `/reposicion` entry:
```typescript
{ path: "/reposiciones", label: "Reposiciones", description: "Reposicion automatica con datos Shopify", icon: PackageSearch, workspace: "operaciones" },
```
Note: `PackageSearch` is already imported at the top of pages.ts.

**src/App.tsx** — Add lazy import and route:
1. Add after the existing `Reposicion` line: `const Reposiciones = lazyWithReload(() => import("./pages/reposiciones"));`
2. Add route inside the protected routes block after `/reposicion`: `<Route path="/reposiciones" element={<Reposiciones />} />`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>hooks.ts has 5 hooks + 1 save function; index.tsx renders placeholder; /reposiciones registered in PAGE_REGISTRY and App.tsx; build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm Firestore rules update for replenishment_config collection</name>
  <files>firestore.rules</files>
  <read_first>
    firestore.rules
  </read_first>
  <action>
Read the current firestore.rules file and present the proposed addition to the user. Per CLAUDE.md: "NO modificar firestore.rules sin confirmacion explicita del usuario". Per RESEARCH.md Pitfall 4: replenishment_config is a new collection that will get PERMISSION_DENIED without a matching rule.

The proposed rule to add (inside the main match block, after the existing `suppliers` rule):

```
    // --- Replenishment Config (per-user settings) ----------------
    // Each user can read/write only their own config document.
    match /replenishment_config/{userId} {
      allow read, write: if isAuthenticated()
                         && request.auth.uid == userId;
    }
```

After user confirms, add this rule to firestore.rules. Do NOT deploy the rules — just update the file. Deployment will happen separately.
  </action>
  <what-built>Firestore rules addition for the new replenishment_config collection needed by CONF-05 (user config persistence). Without this rule, Firestore will deny all reads/writes to replenishment_config/{uid} in production.</what-built>
  <how-to-verify>
    1. Review the proposed Firestore rule shown above
    2. Confirm it matches the security model: each user reads/writes ONLY their own config document (scoped by auth.uid == userId)
    3. Type "approved" to apply the change to firestore.rules
  </how-to-verify>
  <verify>grep -c "replenishment_config" firestore.rules should return 1 or more</verify>
  <done>firestore.rules updated with replenishment_config/{userId} rule scoped to authenticated user's own document</done>
  <resume-signal>Type "approved" to apply the firestore.rules change, or suggest modifications</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/test/reposiciones` passes with all stubs green
- `npm run build` passes with zero errors
- `src/pages/reposiciones/` directory contains types.ts, api.ts, hooks.ts, index.tsx
- `src/test/reposiciones/` directory contains config.test.tsx, table.test.tsx
- PAGE_REGISTRY has `/reposiciones` entry
- App.tsx has lazy import and Route for `/reposiciones`
- firestore.rules contains replenishment_config rule (after user confirmation)
</verification>

<success_criteria>
- All TypeScript types match backend Pydantic models (ProductResult, CalculateRequest, CalculateResponse, etc.)
- API layer covers all 5 endpoint functions with resilientFetch + handleResponse pattern
- 409 handling in refreshSales for OPERATION_IN_PROGRESS
- React Query hooks for locations, vendors, sales polling, calculate mutation
- Firestore config read/write hook and function for replenishment_config/{uid}
- Page registered at /reposiciones in PAGE_REGISTRY (operaciones workspace) and App.tsx
- Test stubs exist and pass for CONF-01 through CONF-05 and APPR-02 through APPR-04
- Firestore rules updated for replenishment_config (with user confirmation)
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-wizard-frontend-config-y-sugeridos/06-01-SUMMARY.md`
</output>
