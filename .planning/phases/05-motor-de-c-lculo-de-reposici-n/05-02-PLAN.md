---
phase: 05-motor-de-c-lculo-de-reposici-n
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/routers/reposiciones.py
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06

must_haves:
  truths:
    - "POST /api/reposiciones/calculate acepta {location_id, vendors, lead_time_days, safety_factor, date_range_days} y devuelve resultados de cálculo"
    - "La respuesta incluye products[], vendor_summary[] y stats con los campos definidos en D-14"
    - "Cuando sales_cache no existe, el endpoint devuelve 424 con mensaje claro en español"
    - "Al finalizar el cálculo, se crea un documento borrador en Firestore replenishment_orders y el response incluye draft_id"
    - "Si vendors es null o lista vacía, incluye todos los proveedores del inventario"
  artifacts:
    - path: "backend/routers/reposiciones.py"
      provides: "POST /calculate endpoint con Pydantic models y Firestore draft write"
      contains: "@router.post(\"/calculate\")"
    - path: "backend/routers/reposiciones.py"
      provides: "Modelos Pydantic: CalculateRequest, ProductAnalysis, VendorSummary, ReplenishmentStats, CalculateResponse"
      contains: "class CalculateRequest(BaseModel)"
  key_links:
    - from: "backend/routers/reposiciones.py"
      to: "backend/services/reposicion_service.py"
      via: "import calculate_replenishment"
      pattern: "from services.reposicion_service import calculate_replenishment"
    - from: "backend/routers/reposiciones.py"
      to: "backend/services/shopify_service.py"
      via: "get_inventory_by_location(location_gid, vendor_filter)"
      pattern: "shopify_service.get_inventory_by_location"
    - from: "POST /calculate handler"
      to: "Firestore replenishment_orders"
      via: "_persist_draft() escribe borrador con status='borrador'"
      pattern: "db.collection(\"replenishment_orders\").document()"
---

<objective>
Agregar el endpoint `POST /api/reposiciones/calculate` al router existente `reposiciones.py`, junto con los modelos Pydantic y la función de persistencia del borrador en Firestore. Este plan AÑADE al archivo existente — no lo reemplaza.

Purpose: Exponer el motor de cálculo del Plan 01 como endpoint HTTP, con validación de entrada, carga de datos y persistencia del resultado.
Output: Endpoint funcional que acepta parámetros de cálculo y devuelve sugeridos con draft_id
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-de-c-lculo-de-reposici-n/05-CONTEXT.md
@.planning/phases/05-motor-de-c-lculo-de-reposici-n/05-RESEARCH.md
@.planning/phases/05-motor-de-c-lculo-de-reposici-n/05-01-SUMMARY.md

@backend/routers/reposiciones.py
@backend/services/reposicion_service.py
</context>

<interfaces>
<!-- Contrato de la función calculate_replenishment (creada en Plan 01) -->
```python
# from backend/services/reposicion_service.py (Plan 01)
def calculate_replenishment(
    inventory_items: list[dict],           # [{sku, title, vendor, available}]
    sales_cache: dict[str, dict[str, int]], # {sku: {"YYYY-MM": int}}
    pending_orders_map: dict[str, list[dict]], # {sku: [{quantity, created_at, ...}]}
    params: dict                           # {lead_time_days, safety_factor, date_range_days, date_range_start, date_range_end}
) -> dict:
    # Returns: {"products": [...], "vendor_summary": [...], "stats": {...}}
```

<!-- Función existente en reposiciones.py para leer Firestore -->
```python
# Existente en backend/routers/reposiciones.py (lines 101-103)
def _get_firestore():
    from services.firebase_service import get_firestore_db
    return get_firestore_db()
```

<!-- Función existente en shopify_service.py -->
```python
# from backend/services/shopify_service.py (verified)
def get_inventory_by_location(
    location_gid: str,
    vendor_filter: list[str] | None = None
) -> list[dict]:  # [{sku, title, vendor, available}]
```

<!-- Estructura del sales_cache en Firestore (Phase 4) -->
```python
# sales_cache/6m_global
{
  "status": "ready",
  "last_refreshed": "ISO 8601",
  "date_range_start": "YYYY-MM-DD",
  "date_range_end": "YYYY-MM-DD",
  "sku_count": int,
  "data": {sku: {"YYYY-MM": int}},  # inline si <= 8000 SKUs
  "chunked": True,  # OR: True si > 8000, datos en subcollection chunks/
}
```

<!-- Estructura de pending orders en Firestore (Phase 5 contract para Phase 7) -->
<!-- SCHEMA CONTRACT: Phase 7 debe escribir replenishment_orders con este schema -->
```python
# replenishment_orders/{id}
{
  "status": "aprobado" | "enviado",
  "created_at": "ISO 8601 UTC string",
  "location_id": str,
  "items": [
    {"sku": str, "vendor": str, "quantity": int, "title": str}
  ]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Modelos Pydantic + helper _load_sales_cache + _load_pending_orders_map</name>
  <files>backend/routers/reposiciones.py</files>

  <read_first>
    - backend/routers/reposiciones.py (LEER COMPLETO antes de tocar — el archivo tiene 455 líneas existentes)
    - .planning/phases/05-motor-de-c-lculo-de-reposici-n/05-RESEARCH.md (Pattern 1, Pattern 4, Pattern 5, Pitfall 3, Pitfall 4, Pitfall 5)
    - .planning/phases/05-motor-de-c-lculo-de-reposici-n/05-CONTEXT.md (D-11 a D-18)
  </read_first>

  <action>
    AÑADIR al final del archivo `backend/routers/reposiciones.py` (después de la última línea `raise HTTPException(status_code=500, detail="Cache en estado inconsistente.")`).

    NO modificar ni borrar nada existente. Solo agregar al final.

    **Sección 1: Imports adicionales** (si no están ya presentes — verificar antes de agregar)
    Verificar si `from pydantic import BaseModel` ya está en el import de la línea 11. Si no está, agregar `BaseModel` al import existente: `from fastapi import APIRouter, HTTPException, Query`.
    Nota: En FastAPI moderno, BaseModel se importa de pydantic, no de fastapi. Agregar al inicio del archivo solo si no existe: `from pydantic import BaseModel`.

    Verificar si `from typing import Optional` ya está (línea 12). Si no, agregar.

    **Sección 2: Constantes nuevas** (agregar junto a las existentes al inicio de la sección de calculate):
    ```python
    _REPLENISHMENT_ORDERS_COLLECTION = "replenishment_orders"
    ```

    **Sección 3: Modelos Pydantic** (agregar como nueva sección en el archivo, antes del endpoint):
    ```python
    # ---------------------------------------------------------------------------
    # Modelos Pydantic — POST /calculate (CALC-01 a CALC-06)
    # ---------------------------------------------------------------------------

    class CalculateRequest(BaseModel):
        location_id: str
        vendors: Optional[list[str]] = None
        lead_time_days: int = 14
        safety_factor: float = 1.5
        date_range_days: int = 180

    class ProductAnalysis(BaseModel):
        sku: str
        title: str
        vendor: str
        classification: str          # "Bestseller" | "Regular" | "Slow" | "Long Tail"
        classification_label: str
        sales_per_month: float
        sales_per_week: float
        sales_per_day: float
        total_sold: int
        stock: int
        days_of_inventory: Optional[float]   # None cuando daily_sales == 0 (D-10, Pitfall 2)
        urgency: str                  # "URGENTE" | "PRONTO" | "NORMAL" | "OK"
        urgency_label: str
        reorder_point: float
        needs_reorder: bool
        suggested_qty: int
        in_transit_real: int

    class VendorSummary(BaseModel):
        vendor: str
        total_skus: int
        total_units_to_order: int
        urgent_count: int

    class ReplenishmentStats(BaseModel):
        total_products: int
        needs_replenishment: int
        urgent: int
        out_of_stock: int
        vendors_with_orders: int

    class CalculateResponse(BaseModel):
        products: list[ProductAnalysis]
        vendor_summary: list[VendorSummary]
        stats: ReplenishmentStats
        draft_id: str                # ID del documento borrador en Firestore (D-13)
    ```

    **Sección 4: Helpers** para carga de datos:

    ```python
    # ---------------------------------------------------------------------------
    # Helpers de datos para /calculate
    # ---------------------------------------------------------------------------

    def _load_sales_cache_data(db) -> tuple[dict, dict]:
        """
        Lee sales_cache/6m_global desde Firestore.
        Maneja tanto almacenamiento inline (<=8000 SKUs) como chunked (>8000 SKUs).
        Returns: (sales_data_dict, cache_meta_dict)
        Raises: HTTPException 424 si el cache no existe o no está listo (Pitfall 4).
        """
        doc = db.collection(_SALES_CACHE_COLLECTION).document(_SALES_CACHE_DOC).get()
        if not doc.exists:
            raise HTTPException(
                status_code=424,
                detail="Cache de ventas no disponible. Ejecuta POST /sales/refresh primero."
            )
        cache_meta = doc.to_dict()
        if cache_meta.get("status") != "ready":
            raise HTTPException(
                status_code=424,
                detail="Cache de ventas en proceso de generación. Espera a que termine."
            )

        if "data" in cache_meta:
            return cache_meta["data"], cache_meta

        if cache_meta.get("chunked"):
            merged = {}
            chunks = db.collection(_SALES_CACHE_COLLECTION).document(_SALES_CACHE_DOC) \
                       .collection("chunks").stream()
            for chunk in chunks:
                merged.update(chunk.to_dict().get("data", {}))
            return merged, cache_meta

        raise HTTPException(status_code=500, detail="Cache en estado inconsistente.")


    def _load_pending_orders_map(db) -> dict[str, list[dict]]:
        """
        Lee replenishment_orders donde status in ['aprobado', 'enviado'].
        Construye mapa {sku: [{quantity, created_at, ...}]} para lookup O(1) en el motor.
        Retorna dict vacío si no hay pedidos pendientes (D-03: correcto en primer run).

        SCHEMA CONTRACT: Los documentos deben tener campo 'items' como lista de
        {sku, quantity, vendor, title} y campo 'created_at' como ISO 8601 UTC string.
        Phase 7 debe escribir replenishment_orders con este schema.
        """
        pending_map: dict[str, list[dict]] = {}
        try:
            # Pitfall 3: filtrar explícitamente por status para no inflar in-transit
            docs = db.collection(_REPLENISHMENT_ORDERS_COLLECTION) \
                     .where("status", "in", ["aprobado", "enviado"]) \
                     .stream()
            for doc in docs:
                order = doc.to_dict()
                created_at_str = order.get("created_at", "")
                items = order.get("items", [])
                for item in items:
                    sku = item.get("sku", "")
                    qty = item.get("quantity", 0)
                    if not sku or qty <= 0:
                        continue
                    if sku not in pending_map:
                        pending_map[sku] = []
                    pending_map[sku].append({
                        "quantity": qty,
                        "created_at": created_at_str,
                    })
        except Exception:
            pass  # Colección vacía o no existe — correcto en primer run (D-03)
        return pending_map


    def _persist_draft(db, result: dict, request_params: dict) -> str:
        """
        Crea documento borrador en replenishment_orders (D-13).
        Retorna el ID del documento creado.
        """
        doc_ref = db.collection(_REPLENISHMENT_ORDERS_COLLECTION).document()
        doc_ref.set({
            "status": "borrador",
            "created_at": datetime.now(timezone.utc).isoformat(),
            "params": request_params,
            "products": result["products"],
            "vendor_summary": result["vendor_summary"],
            "stats": result["stats"],
        })
        return doc_ref.id
    ```
  </action>

  <verify>
    <automated>cd "C:/Users/opera/OneDrive/Documentos/BUKZ/08 - PYTHON/BukzBrainv2-main" && python -c "import sys; sys.path.insert(0, 'backend'); from routers import reposiciones; print('Import OK')" 2>&1 || echo "Verificar manualmente si falta PYTHONPATH"</automated>
  </verify>

  <acceptance_criteria>
    - `backend/routers/reposiciones.py` contiene `class CalculateRequest(BaseModel)`
    - `backend/routers/reposiciones.py` contiene `class CalculateResponse(BaseModel)`
    - `backend/routers/reposiciones.py` contiene `def _load_sales_cache_data(`
    - `backend/routers/reposiciones.py` contiene `def _load_pending_orders_map(`
    - `backend/routers/reposiciones.py` contiene `def _persist_draft(`
    - `backend/routers/reposiciones.py` contiene `_REPLENISHMENT_ORDERS_COLLECTION = "replenishment_orders"`
    - Los endpoints existentes (GET /locations, GET /vendors, GET /inventory, POST /sales/refresh, GET /sales/status, GET /sales/data) NO fueron modificados — verificar con `grep "@router" backend/routers/reposiciones.py`
    - `from pydantic import BaseModel` presente en imports
  </acceptance_criteria>

  <done>Modelos Pydantic y helpers de datos añadidos al archivo. Archivo importable sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint POST /calculate con integración completa</name>
  <files>backend/routers/reposiciones.py</files>

  <read_first>
    - backend/routers/reposiciones.py (estado actual después de Task 1 — releer)
    - backend/services/reposicion_service.py (funciones disponibles: calculate_replenishment)
    - .planning/phases/05-motor-de-c-lculo-de-reposici-n/05-CONTEXT.md (D-11, D-12, D-13, D-14, D-15, D-16)
    - .planning/phases/05-motor-de-c-lculo-de-reposici-n/05-RESEARCH.md (Anti-Patterns, Open Question 2 sobre date_range coverage)
  </read_first>

  <action>
    AÑADIR al final de `backend/routers/reposiciones.py`, después de los helpers añadidos en Task 1:

    ```python
    # ---------------------------------------------------------------------------
    # CALC-01 a CALC-06: Motor de Cálculo de Reposición
    # ---------------------------------------------------------------------------

    @router.post("/calculate", response_model=CalculateResponse)
    def calculate_replenishment_endpoint(body: CalculateRequest):
        """
        Calcula cantidades sugeridas de reposición por SKU.

        Flujo:
        1. Cargar inventario real desde Shopify (get_inventory_by_location)
        2. Cargar cache de ventas desde Firestore (sales_cache/6m_global) — 424 si no existe
        3. Cargar pedidos pendientes desde Firestore (replenishment_orders donde status in [aprobado, enviado])
        4. Calcular sugeridos con calculate_replenishment() del servicio
        5. Persistir resultado como borrador en Firestore replenishment_orders
        6. Devolver resultado completo con draft_id

        CALC-01: formula suggested_qty = max(0, ceil((daily*lt*sf) - stock - transit))
        CALC-02: clasificación por velocidad de ventas
        CALC-03: urgencia por días de inventario
        CALC-04+CALC-05: en_tránsito real via absorción de ventas desde fecha pedido
        CALC-06: agregación por proveedor
        """
        from services.reposicion_service import calculate_replenishment

        db = _get_firestore()

        # Step 1: Inventario real de Shopify (D-15: TODOS los productos de la sede)
        try:
            inventory_items = shopify_service.get_inventory_by_location(
                location_gid=body.location_id,
                vendor_filter=body.vendors if body.vendors else None,  # D-16
            )
        except Exception as e:
            raise HTTPException(status_code=502, detail=f"Error obteniendo inventario de Shopify: {str(e)}")

        if not inventory_items:
            raise HTTPException(
                status_code=404,
                detail=f"No se encontró inventario para location_id={body.location_id}"
                       + (f" con vendors={body.vendors}" if body.vendors else "")
            )

        # Step 2: Cache de ventas desde Firestore (424 si no existe — Pitfall 4)
        sales_data, cache_meta = _load_sales_cache_data(db)

        # Open Question 2: respetar date_range_days del request vs cobertura del cache
        # date_range_end = hoy; date_range_start = max(cache_start, hoy - date_range_days)
        from datetime import date as date_type
        today = datetime.now(timezone.utc).date()
        requested_start = today - timedelta(days=body.date_range_days)
        cache_start_str = cache_meta.get("date_range_start", "")
        try:
            cache_start = date_type.fromisoformat(cache_start_str)
        except (ValueError, TypeError):
            cache_start = requested_start
        effective_start = max(requested_start, cache_start)

        # Step 3: Pedidos pendientes para detección de en-tránsito (D-04)
        pending_orders_map = _load_pending_orders_map(db)

        # Step 4: Cálculo
        params = {
            "lead_time_days": body.lead_time_days,
            "safety_factor": body.safety_factor,
            "date_range_days": body.date_range_days,
            "date_range_start": effective_start,
            "date_range_end": today,
        }

        try:
            result = calculate_replenishment(
                inventory_items=inventory_items,
                sales_cache=sales_data,
                pending_orders_map=pending_orders_map,
                params=params,
            )
        except Exception as e:
            raise HTTPException(status_code=500, detail=f"Error en cálculo de reposición: {str(e)}")

        # Step 5: Persistir borrador en Firestore (D-13)
        request_params = {
            "location_id": body.location_id,
            "vendors": body.vendors,
            "lead_time_days": body.lead_time_days,
            "safety_factor": body.safety_factor,
            "date_range_days": body.date_range_days,
        }
        try:
            draft_id = _persist_draft(db, result, request_params)
        except Exception as e:
            raise HTTPException(status_code=500, detail=f"Error guardando borrador en Firestore: {str(e)}")

        # Step 6: Respuesta (D-14: structure mirrors TypeScript ReplenishmentResult)
        return CalculateResponse(
            products=[ProductAnalysis(**p) for p in result["products"]],
            vendor_summary=[VendorSummary(**v) for v in result["vendor_summary"]],
            stats=ReplenishmentStats(**result["stats"]),
            draft_id=draft_id,
        )
    ```

    IMPORTANTE: No modificar nada más del archivo. Solo añadir este bloque al final.
  </action>

  <verify>
    <automated>npx vitest run src/test/replenishment-calc.test.ts && grep -c "@router" backend/routers/reposiciones.py</automated>
  </verify>

  <acceptance_criteria>
    - `backend/routers/reposiciones.py` contiene `@router.post("/calculate"` exactamente una vez
    - `backend/routers/reposiciones.py` contiene `from services.reposicion_service import calculate_replenishment`
    - `backend/routers/reposiciones.py` contiene `_load_sales_cache_data(db)`
    - `backend/routers/reposiciones.py` contiene `_load_pending_orders_map(db)`
    - `backend/routers/reposiciones.py` contiene `_persist_draft(db, result, request_params)`
    - `backend/routers/reposiciones.py` contiene `status_code=424` (guard para cache inexistente — Pitfall 4)
    - `backend/routers/reposiciones.py` contiene `vendor_filter=body.vendors if body.vendors else None` (D-16)
    - El archivo contiene exactamente 7 rutas `@router.` (6 existentes + 1 nueva)
    - `grep "@router" backend/routers/reposiciones.py` devuelve 7 líneas
    - `npx vitest run src/test/replenishment-calc.test.ts` sigue verde (sin regresiones)
  </acceptance_criteria>

  <done>POST /api/reposiciones/calculate funcional: acepta parámetros, calcula sugeridos, persiste borrador, devuelve CalculateResponse con draft_id</done>
</task>

</tasks>

<verification>
Verificación end-to-end del plan completo:

```bash
# 1. Tests de cálculo siguen verdes
npx vitest run src/test/replenishment-calc.test.ts

# 2. Archivo router tiene exactamente 7 rutas (6 existentes + calculate)
grep -c "@router\." backend/routers/reposiciones.py

# 3. Endpoint calculate presente
grep "@router.post(\"/calculate\")" backend/routers/reposiciones.py

# 4. Sin regresión en rutas existentes
grep "@router" backend/routers/reposiciones.py

# 5. Import del servicio presente
grep "from services.reposicion_service import" backend/routers/reposiciones.py

# 6. Guard 424 para cache ausente presente
grep "status_code=424" backend/routers/reposiciones.py

# 7. Schema contract documentado
grep "SCHEMA CONTRACT" backend/routers/reposiciones.py backend/services/reposicion_service.py
```
</verification>

<success_criteria>
- `POST /api/reposiciones/calculate` responde con `products[]`, `vendor_summary[]`, `stats`, `draft_id`
- Cuando sales_cache no existe, responde 424 con mensaje en español
- El borrador se persiste en `replenishment_orders` con `status: "borrador"`
- Los 6 endpoints existentes siguen intactos
- `npx vitest run src/test/` pasa sin regresiones
</success_criteria>

<output>
After completion, create `.planning/phases/05-motor-de-c-lculo-de-reposici-n/05-02-SUMMARY.md`
</output>
