---
phase: 04-pipeline-de-datos-shopify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routers/reposiciones.py
  - backend/services/shopify_service.py
  - backend/main.py
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03

must_haves:
  truths:
    - "GET /api/reposiciones/locations devuelve lista de sedes con id y nombre"
    - "GET /api/reposiciones/vendors devuelve lista de proveedores con conteo de productos"
    - "GET /api/reposiciones/inventory?location_id=X&vendors[]=Y devuelve niveles de stock filtrados"
  artifacts:
    - path: "backend/routers/reposiciones.py"
      provides: "Router con endpoints locations, vendors, inventory"
      contains: "APIRouter(prefix=\"/api/reposiciones\")"
    - path: "backend/services/shopify_service.py"
      provides: "get_vendors_from_shopify() y get_inventory_by_location()"
      exports: ["get_vendors_from_shopify", "get_inventory_by_location"]
    - path: "backend/main.py"
      provides: "Router reposiciones registrado"
      contains: "from routers import reposiciones"
  key_links:
    - from: "backend/routers/reposiciones.py"
      to: "backend/services/shopify_service.py"
      via: "import shopify_service functions"
      pattern: "get_vendors_from_shopify|get_inventory_by_location|get_locations"
    - from: "backend/main.py"
      to: "backend/routers/reposiciones.py"
      via: "app.include_router"
      pattern: "include_router\\(reposiciones\\.router"
---

<objective>
Crear el router `reposiciones.py` con los tres endpoints simples de datos maestros: locations (sedes), vendors (proveedores) y inventory (niveles de stock por sede/proveedor). Registrar el router en main.py. Agregar las dos funciones nuevas en shopify_service.py.

Purpose: Sin estos endpoints, el frontend de Phase 6 no puede poblar el dropdown de sedes ni el multi-select de proveedores. Son los bloques más simples y establecen el patrón del router para Plan 02.
Output: 3 endpoints GET funcionales bajo /api/reposiciones, router registrado, dos nuevas funciones en shopify_service.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pipeline-de-datos-shopify/04-CONTEXT.md

<!-- Interfaces clave que el ejecutor necesita — extraídas del codebase -->
<interfaces>
<!-- De backend/services/shopify_service.py (funciones existentes a REUSAR) -->
```python
# Devuelve dict {nombre_sede: id_sede}
def get_locations() -> dict:
    ...

# Rate limiter singleton — siempre llamar _throttler.wait_if_needed() antes de cada request
_throttler = ShopifyThrottler()

# settings.get_graphql_url() -> str  (URL GraphQL de Shopify)
# settings.get_shopify_headers() -> dict  (headers con X-Shopify-Access-Token)
```

<!-- De backend/main.py (patrón de registro de routers) -->
```python
_auth = [Depends(verify_firebase_token)]
app.include_router(ingreso.router, dependencies=_auth)
# Pattern: app.include_router(reposiciones.router, dependencies=_auth)
```

<!-- De backend/routers/ingreso.py (patrón de router) -->
```python
from fastapi import APIRouter
router = APIRouter(prefix="/api/ingreso", tags=["Ingreso Mercancía"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Agregar funciones get_vendors_from_shopify() y get_inventory_by_location() a shopify_service.py</name>
  <files>backend/services/shopify_service.py</files>

  <read_first>
    - backend/services/shopify_service.py (leer completo — especialmente get_locations() línea 113 y _throttler setup para entender el patrón de GraphQL paginado y rate limiting)
    - backend/config.py (verificar métodos disponibles: settings.get_graphql_url(), settings.get_shopify_headers())
  </read_first>

  <action>
Agregar al final de `backend/services/shopify_service.py` (DESPUÉS de la función `load_sales_sync`) las dos funciones nuevas:

**Función 1: `get_vendors_from_shopify()`**

```python
def get_vendors_from_shopify() -> list[dict]:
    """
    Devuelve lista de proveedores únicos desde productos de Shopify con conteo de productos.
    Pagina con cursor hasta obtener todos los productos (Bukz tiene 150+ proveedores).
    Returns: [{"name": "Editorial X", "product_count": 42}, ...]
    """
    graphql_url = settings.get_graphql_url()
    headers = settings.get_shopify_headers()

    vendor_counts: dict[str, int] = {}
    cursor = None

    while True:
        after_clause = f', after: "{cursor}"' if cursor else ""
        query = """
        {
          products(first: 250%s) {
            pageInfo { hasNextPage endCursor }
            edges {
              node { vendor }
            }
          }
        }
        """ % after_clause

        _throttler.wait_if_needed()
        try:
            resp = requests.post(
                graphql_url,
                json={"query": query},
                headers=headers,
                timeout=30,
            )
            _throttler.update_from_response(resp)
            if resp.status_code != 200:
                break

            data = resp.json().get("data", {}).get("products", {})
            for edge in data.get("edges", []):
                vendor = (edge.get("node") or {}).get("vendor", "").strip()
                if vendor:
                    vendor_counts[vendor] = vendor_counts.get(vendor, 0) + 1

            page_info = data.get("pageInfo", {})
            if not page_info.get("hasNextPage"):
                break
            cursor = page_info.get("endCursor")
        except Exception:
            break

    return [{"name": v, "product_count": c} for v, c in sorted(vendor_counts.items())]
```

**Función 2: `get_inventory_by_location()`**

```python
def get_inventory_by_location(location_gid: str, vendor_filter: list[str] | None = None) -> list[dict]:
    """
    Devuelve niveles de inventario (available) para todos los productos en una sede de Shopify.
    Filtra por vendor si vendor_filter no es None ni vacío.

    Args:
        location_gid: GID de Shopify, e.g. "gid://shopify/Location/12345"
        vendor_filter: lista de nombres de proveedor exactos; None o [] = todos

    Returns: [{"sku": str, "title": str, "vendor": str, "available": int}, ...]
    """
    graphql_url = settings.get_graphql_url()
    headers = settings.get_shopify_headers()

    results: list[dict] = []
    cursor = None

    while True:
        after_clause = f', after: "{cursor}"' if cursor else ""
        query = """
        {
          location(id: "%s") {
            inventoryLevels(first: 250%s) {
              pageInfo { hasNextPage endCursor }
              edges {
                node {
                  quantities(names: ["available"]) {
                    name
                    quantity
                  }
                  item {
                    sku
                    variant {
                      product {
                        vendor
                        title
                      }
                    }
                  }
                }
              }
            }
          }
        }
        """ % (location_gid, after_clause)

        _throttler.wait_if_needed()
        try:
            resp = requests.post(
                graphql_url,
                json={"query": query},
                headers=headers,
                timeout=30,
            )
            _throttler.update_from_response(resp)
            if resp.status_code != 200:
                break

            location_data = resp.json().get("data", {}).get("location") or {}
            inv_levels = location_data.get("inventoryLevels", {})

            for edge in inv_levels.get("edges", []):
                node = edge.get("node", {})
                item = node.get("item", {})
                sku = (item.get("sku") or "").strip()
                variant = (item.get("variant") or {})
                product = (variant.get("product") or {})
                vendor = (product.get("vendor") or "").strip()
                title = (product.get("title") or "").strip()

                # Filtrar por vendor si se especificó
                if vendor_filter and vendor not in vendor_filter:
                    continue

                available = 0
                for qty_entry in node.get("quantities", []):
                    if qty_entry.get("name") == "available":
                        available = int(qty_entry.get("quantity") or 0)
                        break

                if sku:
                    results.append({
                        "sku": sku,
                        "title": title,
                        "vendor": vendor,
                        "available": available,
                    })

            page_info = inv_levels.get("pageInfo", {})
            if not page_info.get("hasNextPage"):
                break
            cursor = page_info.get("endCursor")
        except Exception:
            break

    return results
```
  </action>

  <verify>
    <automated>cd "C:/Users/opera/OneDrive/Documentos/BUKZ/08 - PYTHON/BukzBrainv2-main/backend" && python -c "from services.shopify_service import get_vendors_from_shopify, get_inventory_by_location; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - backend/services/shopify_service.py contiene `def get_vendors_from_shopify()` (grep verifiable)
    - backend/services/shopify_service.py contiene `def get_inventory_by_location(` (grep verifiable)
    - `get_vendors_from_shopify` contiene `pageInfo` y `hasNextPage` (paginación implementada)
    - `get_inventory_by_location` contiene `quantities(names: ["available"])` en el GraphQL query
    - `get_inventory_by_location` contiene `if vendor_filter and vendor not in vendor_filter` (filtro por proveedor)
    - El import `from services.shopify_service import get_vendors_from_shopify, get_inventory_by_location` ejecuta sin error
  </acceptance_criteria>

  <done>Las dos funciones existen en shopify_service.py, son importables sin error, y no introducen nuevas dependencias.</done>
</task>

<task type="auto">
  <name>Tarea 2: Crear router reposiciones.py con endpoints locations, vendors, inventory; registrar en main.py</name>
  <files>backend/routers/reposiciones.py, backend/main.py</files>

  <read_first>
    - backend/main.py (leer completo — ver patrón _auth y todos los include_router existentes)
    - backend/routers/ingreso.py (leer líneas 1-50 — ver patrón APIRouter con prefix y tags)
    - backend/services/shopify_service.py (confirmar que get_locations, get_vendors_from_shopify, get_inventory_by_location existen tras Tarea 1)
  </read_first>

  <action>
**Crear `backend/routers/reposiciones.py` completo:**

```python
"""
Router de Reposiciones — Pipeline de Datos Shopify.
Expone endpoints para locations, vendors, inventario y cache de ventas (bulk ops).
Plan 01: locations, vendors, inventory
Plan 02: sales/refresh, sales/status, sales/data (bulk ops + cache)
"""
from fastapi import APIRouter, HTTPException, Query
from typing import Optional

from services import shopify_service

router = APIRouter(prefix="/api/reposiciones", tags=["Reposiciones"])


# ---------------------------------------------------------------------------
# SHOP-01: Locations (sedes)
# ---------------------------------------------------------------------------

@router.get("/locations")
def get_locations():
    """
    Devuelve lista de sedes (Locations) de Shopify.
    Returns: [{"name": str, "id": str}, ...]
    """
    try:
        locations_dict = shopify_service.get_locations()
        # get_locations() devuelve {name: id} — convertir a lista de objetos
        return [{"name": name, "id": loc_id} for name, loc_id in locations_dict.items()]
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Error obteniendo sedes: {str(e)}")


# ---------------------------------------------------------------------------
# SHOP-03: Vendors (proveedores)
# ---------------------------------------------------------------------------

@router.get("/vendors")
def get_vendors():
    """
    Devuelve lista de proveedores únicos desde productos de Shopify con conteo.
    Returns: [{"name": str, "product_count": int}, ...]
    """
    try:
        return shopify_service.get_vendors_from_shopify()
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Error obteniendo proveedores: {str(e)}")


# ---------------------------------------------------------------------------
# SHOP-02: Inventory por location y vendor
# ---------------------------------------------------------------------------

@router.get("/inventory")
def get_inventory(
    location_id: str = Query(..., description="GID de la sede, e.g. gid://shopify/Location/12345"),
    vendors: Optional[list[str]] = Query(default=None, alias="vendors[]", description="Filtro de proveedores"),
):
    """
    Devuelve niveles de inventario (available) para una sede, opcionalmente filtrados por proveedor.
    Query params: location_id (requerido), vendors[] (opcional, repetible)
    Returns: [{"sku": str, "title": str, "vendor": str, "available": int}, ...]
    """
    if not location_id:
        raise HTTPException(status_code=422, detail="location_id es requerido")

    try:
        return shopify_service.get_inventory_by_location(
            location_gid=location_id,
            vendor_filter=vendors if vendors else None,
        )
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Error obteniendo inventario: {str(e)}")
```

**Modificar `backend/main.py`:**

Agregar en la sección de imports (después de `from routers import webhooks`):
```python
from routers import reposiciones
```

Agregar en la sección de routers protegidos (después de `app.include_router(suppliers.router, dependencies=_auth)`):
```python
app.include_router(reposiciones.router, dependencies=_auth)
```

El bloque de webhooks sin auth (`app.include_router(webhooks.router)`) debe quedar DESPUÉS del nuevo router.
  </action>

  <verify>
    <automated>cd "C:/Users/opera/OneDrive/Documentos/BUKZ/08 - PYTHON/BukzBrainv2-main/backend" && python -c "from routers.reposiciones import router; print('router prefix:', router.prefix)"</automated>
  </verify>

  <acceptance_criteria>
    - backend/routers/reposiciones.py existe y contiene `router = APIRouter(prefix="/api/reposiciones"`
    - backend/routers/reposiciones.py contiene `@router.get("/locations")`
    - backend/routers/reposiciones.py contiene `@router.get("/vendors")`
    - backend/routers/reposiciones.py contiene `@router.get("/inventory")`
    - backend/main.py contiene `from routers import reposiciones`
    - backend/main.py contiene `app.include_router(reposiciones.router, dependencies=_auth)`
    - `from routers.reposiciones import router` ejecuta sin error
    - `python -c "from routers.reposiciones import router; print(router.prefix)"` imprime `/api/reposiciones`
  </acceptance_criteria>

  <done>Router existe, todos los endpoints responden a rutas correctas bajo /api/reposiciones, está registrado en main.py con autenticación Firebase.</done>
</task>

</tasks>

<verification>
Con ambas tareas completas, verificar integración end-to-end del módulo importando la app completa:

```bash
cd "C:/Users/opera/OneDrive/Documentos/BUKZ/08 - PYTHON/BukzBrainv2-main/backend"
python -c "from main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'reposicion' in r])"
```

Debe imprimir: `['/api/reposiciones/locations', '/api/reposiciones/vendors', '/api/reposiciones/inventory']`
</verification>

<success_criteria>
1. `GET /api/reposiciones/locations` — endpoint definido, llama `shopify_service.get_locations()`, devuelve lista `[{name, id}]`
2. `GET /api/reposiciones/vendors` — endpoint definido, llama `get_vendors_from_shopify()` (paginado), devuelve `[{name, product_count}]`
3. `GET /api/reposiciones/inventory?location_id=X` — endpoint definido, llama `get_inventory_by_location()`, acepta `vendors[]` opcional
4. Router importable desde main.py sin errores
5. Ninguna dependencia nueva agregada (solo stdlib + requests + fastapi ya en requirements.txt)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/04-pipeline-de-datos-shopify/04-01-SUMMARY.md`
</output>
